import { MatchesProgressPipe } from './mathces-progress.pipe';

describe('MatchesProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchesProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
