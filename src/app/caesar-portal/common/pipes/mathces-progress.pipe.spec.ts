import { MathcesProgressPipe } from './mathces-progress.pipe';

describe('MathcesProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new MathcesProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
